Conventions and implementation notes
====================================

.. note::

   This page is being written.

This page records the conventions Asymmetry follows — units, sign and phase
conventions, and the definitions behind the fitted parameters — together with
notes on how the key methods are implemented and where to find the underlying
code and study notes. For the API itself, see :doc:`/api/index`.
