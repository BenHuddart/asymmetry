# Structural transitions: comparison

No reference tool ships dedicated structural-transition
detection. The workflow is captured by general parameter-trending
in all four programs (Mantid Results Tab, musrfit `msr2data`,
WiMDA fit-table, Asymmetry parameter-trending panel).

This candidate would add **kink-finder** style helpers on top of
the existing parameter-trending panel rather than new fit
functions.
