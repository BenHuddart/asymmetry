"""Shared two-panel function-builder foundation.

The searchable component library (:mod:`.library_panel`), the structured
model-row editor (:mod:`.model_rows`), and the dialog shell that combines
them (:mod:`.dialog`). Both the fit-function builder and the parameter-model
builder are built on this package; import from the submodules directly.
"""
