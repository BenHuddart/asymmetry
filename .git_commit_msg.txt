feat: data browser context menu, project persistence, and missing-file recovery

Data browser:
- Right-click context menu on rows: co-add selected, separate combined,
  delete entry/entries (replaces coadd/separate buttons)
- Co-add and separate now insert results at the original row position
  rather than appending to the end
- Fixed context menu not appearing (isRowSelected API error, context menu
  policy set on both table and viewport)

Project persistence:
- Fit panel saves/restores result HTML for single and global tabs
- Fit panel saves/restores active tab index
- Fitted-parameters panel now has full get_state / restore_state
- Docks auto-open on project load when saved results exist
- Schema defaults for fit_ui_state and fit_parameters_state added

Missing-file recovery:
- On project open, if source files cannot be found a dialog prompts the
  user to locate the directory; all missing files are re-resolved using
  the chosen directory by filename match
- Path resolution order: absolute path -> relative to .asymp -> user-
  chosen fallback directory

Tests (154 passing):
- Data browser: context menu co-add/separate/delete; position-aware
  insertion regression tests
- Project schema: fit result round-trip, fitted-params round-trip,
  auto-open dock, missing-file warning, locate-moved-files recovery

Documentation:
- gui_usage.rst: context menu section, updated co-add/separate/delete
  instructions, corrected what is/is not saved, locate-directory dialog,
  removed non-existent search-filter section
- project.rst / data_processing.rst: updated schema example, path
  resolution strategy
- README.md: updated data browser feature bullet
